# WallpaperDuo V2 重构实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 WallpaperDuo 重构为 Cloudflare Workers 全栈应用，解决 CORS 问题，迁移 UI 到 shadcn/ui。

**Architecture:** 前端 React + shadcn/ui + Tailwind，后端 Hono 运行于 Cloudflare Workers，同源部署消除 CORS。

**Tech Stack:** React 19, TypeScript, Vite, shadcn/ui, Tailwind CSS, Zustand, TanStack Query, Hono, Cloudflare Workers

---

## 文件结构规划

### 新建文件

```
wallpaperduo/
├── worker/                       # Cloudflare Worker 后端
│   └── index.ts                  # Hono 应用入口 + API 路由
├── src/
│   ├── components/ui/            # shadcn/ui 组件
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── dialog.tsx
│   │   ├── input.tsx
│   │   ├── select.tsx
│   │   ├── switch.tsx
│   │   ├── tabs.tsx
│   │   ├── toast.tsx
│   │   └── ...
│   ├── lib/
│   │   └── utils.ts              # cn() 工具函数
│   └── styles/
│       └── globals.css           # Tailwind + CSS 变量
├── tailwind.config.ts            # Tailwind 配置
├── postcss.config.js             # PostCSS 配置
├── components.json               # shadcn/ui 配置
└── wrangler.toml                 # Cloudflare Workers 配置
```

### 复用/迁移文件

| 原文件 | 处理方式 |
|--------|----------|
| `src/store/useWorkflowStore.ts` | 迁移，移除 MUI 依赖 |
| `src/store/useSettingsStore.ts` | 迁移，简化 Provider 配置 |
| `src/services/alignment/` | 保留 |
| `src/services/canvas/prepareCanvas.ts` | 保留 |
| `src/services/export/ddwExporter.ts` | 保留 |
| `src/services/prompt/promptPlanner.ts` | 保留 |
| `src/types/domain.ts` | 保留，移除 ComfyUI 类型 |
| `src/i18n/` | 保留 |
| `src/services/providers/*.ts` | 删除，逻辑移到 Worker |

---

## Chunk 1: 项目初始化与配置

### Task 1.1: 创建 v2 分支

**Files:**
- Git 操作

- [ ] **Step 1: 创建并切换到 v2 分支**

```bash
cd "F:/mrbbbaixue/wallpaperduo"
git checkout -b v2
```

Expected: 成功创建并切换到 v2 分支

- [ ] **Step 2: 验证分支**

```bash
git branch
```

Expected: 显示 `* v2`

---

### Task 1.2: 安装 Tailwind CSS

**Files:**
- Create: `tailwind.config.ts`
- Create: `postcss.config.js`
- Modify: `package.json`
- Create: `src/styles/globals.css`

- [ ] **Step 1: 安装 Tailwind 依赖**

```bash
cd "F:/mrbbbaixue/wallpaperduo"
pnpm add -D tailwindcss postcss autoprefixer
```

Expected: 成功安装 tailwindcss, postcss, autoprefixer

- [ ] **Step 2: 初始化 Tailwind**

```bash
npx tailwindcss init -t
```

Expected: 创建 `tailwind.config.ts` 和 `postcss.config.js`

- [ ] **Step 3: 配置 tailwind.config.ts**

```typescript
import type { Config } from "tailwindcss";

export default {
  darkMode: ["class"],
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;
```

- [ ] **Step 4: 安装 tailwindcss-animate**

```bash
pnpm add -D tailwindcss-animate
```

- [ ] **Step 5: 创建 postcss.config.js**

```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 6: 创建 src/styles/globals.css**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --secondary: 210 40% 96.1%;
    --secondary-foreground: 222.2 47.4% 11.2%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --accent: 210 40% 96.1%;
    --accent-foreground: 222.2 47.4% 11.2%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 222.2 84% 4.9%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

- [ ] **Step 7: 提交**

```bash
git add tailwind.config.ts postcss.config.js src/styles/globals.css package.json
git commit -m "chore: 配置 Tailwind CSS

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 1.3: 配置 shadcn/ui

**Files:**
- Create: `components.json`
- Create: `src/lib/utils.ts`
- Modify: `tsconfig.json`
- Modify: `vite.config.ts`

- [ ] **Step 1: 安装 shadcn/ui 必需依赖**

```bash
pnpm add class-variance-authority clsx tailwind-merge lucide-react
```

- [ ] **Step 2: 更新 tsconfig.json 添加路径别名**

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

- [ ] **Step 3: 更新 vite.config.ts 添加路径解析**

```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tsconfigPaths from "vite-tsconfig-paths";
import path from "path";

export default defineConfig({
  plugins: [react(), tsconfigPaths()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  // ... 其他配置
});
```

- [ ] **Step 4: 创建 components.json**

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/styles/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

- [ ] **Step 5: 创建 src/lib/utils.ts**

```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 6: 提交**

```bash
git add components.json src/lib/utils.ts tsconfig.json vite.config.ts package.json
git commit -m "chore: 配置 shadcn/ui

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 1.4: 配置 Cloudflare Workers + Hono

**Files:**
- Create: `wrangler.toml`
- Create: `worker/index.ts`
- Modify: `package.json`
- Modify: `vite.config.ts`

- [ ] **Step 1: 安装 Hono 和 Wrangler**

```bash
pnpm add hono
pnpm add -D wrangler @cloudflare/workers-types
```

- [ ] **Step 2: 创建 wrangler.toml**

```toml
name = "wallpaperduo"
compatibility_date = "2025-04-01"
main = "./dist/worker/index.js"

[assets]
directory = "./dist/client/"
run_worker_first = true

[vars]
ENVIRONMENT = "development"
```

- [ ] **Step 3: 创建 worker/index.ts**

```typescript
import { Hono } from "hono";
import { cors } from "hono/cors";

const app = new Hono();

// CORS 中间件
app.use(
  "/api/*",
  cors({
    origin: "*",
    allowMethods: ["GET", "POST", "OPTIONS"],
    allowHeaders: ["Content-Type", "Authorization"],
  })
);

// 健康检查
app.get("/api/health", (c) => {
  return c.json({ status: "ok", timestamp: new Date().toISOString() });
});

// 测试连接
app.post("/api/test-connection", async (c) => {
  const body = await c.req.json();
  // TODO: 实现 Provider 连接测试
  return c.json({ ok: true, message: "Connection test not implemented" });
});

// 图像分析
app.post("/api/analyze", async (c) => {
  const body = await c.req.json();
  // TODO: 实现图像分析代理
  return c.json({ error: "Not implemented" }, 501);
});

// 图像生成
app.post("/api/generate", async (c) => {
  const body = await c.req.json();
  // TODO: 实现图像生成代理
  return c.json({ error: "Not implemented" }, 501);
});

export default app;
```

- [ ] **Step 4: 更新 package.json 添加脚本**

```json
{
  "scripts": {
    "dev": "vite",
    "dev:worker": "wrangler dev",
    "build": "tsc -b && vite build && tsc -p worker/tsconfig.json",
    "deploy": "wrangler deploy",
    "lint": "eslint .",
    "format": "prettier --write .",
    "preview": "vite preview"
  }
}
```

- [ ] **Step 5: 创建 worker/tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "outDir": "../dist/worker",
    "rootDir": ".",
    "types": ["@cloudflare/workers-types"]
  },
  "include": ["./**/*.ts"]
}
```

- [ ] **Step 6: 提交**

```bash
git add wrangler.toml worker/ package.json vite.config.ts
git commit -m "feat: 配置 Cloudflare Workers + Hono 后端

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Chunk 2: shadcn/ui 组件导入

### Task 2.1: 导入基础 UI 组件

**Files:**
- Create: `src/components/ui/button.tsx`
- Create: `src/components/ui/card.tsx`
- Create: `src/components/ui/dialog.tsx`
- Create: `src/components/ui/input.tsx`
- Create: `src/components/ui/label.tsx`
- Create: `src/components/ui/select.tsx`
- Create: `src/components/ui/switch.tsx`
- Create: `src/components/ui/tabs.tsx`
- Create: `src/components/ui/toast.tsx`
- Create: `src/components/ui/toaster.tsx`
- Create: `src/components/ui/skeleton.tsx`
- Create: `src/components/ui/progress.tsx`
- Create: `src/hooks/use-toast.ts`

- [ ] **Step 1: 使用 shadcn CLI 添加组件**

```bash
npx shadcn@latest add button card dialog input label select switch tabs toast skeleton progress
```

Expected: 在 `src/components/ui/` 创建组件文件

- [ ] **Step 2: 验证组件导入**

检查 `src/components/ui/` 目录，确认所有组件文件已创建。

- [ ] **Step 3: 提交**

```bash
git add src/components/ui/ src/hooks/
git commit -m "feat: 导入 shadcn/ui 基础组件

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Chunk 3: Worker API 实现

### Task 3.1: 实现 Provider 请求适配器

**Files:**
- Create: `worker/providers.ts`
- Create: `worker/types.ts`

- [ ] **Step 1: 创建 worker/types.ts**

```typescript
export interface ProviderConfig {
  baseUrl: string;
  apiKey: string;
  model: string;
  visionModel?: string;
  generateUrl?: string; // 阿里云专用
}

export interface AnalyzeRequest {
  image: string; // base64 data URL
  provider: ProviderConfig;
  prompt?: string;
}

export interface GenerateRequest {
  image: string;
  prompt: string;
  negativePrompt?: string;
  width: number;
  height: number;
  provider: ProviderConfig;
}

export interface SceneAnalysis {
  summary: string;
  subjects: string[];
  foreground: string[];
  background: string[];
  lighting: string;
  palette: string[];
  risks: string[];
}
```

- [ ] **Step 2: 创建 worker/providers.ts**

```typescript
import type { ProviderConfig, SceneAnalysis } from "./types";

export async function testConnection(config: ProviderConfig): Promise<{ ok: boolean; message: string }> {
  const url = `${config.baseUrl}/chat/completions`;
  const model = config.visionModel || config.model;

  try {
    const response = await fetch(url, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${config.apiKey}`,
      },
      body: JSON.stringify({
        model,
        messages: [{ role: "user", content: "ping" }],
        max_tokens: 1,
      }),
    });

    if (response.status === 401) {
      return { ok: false, message: "Invalid API key" };
    }
    if (response.status === 403) {
      return { ok: false, message: "Access forbidden" };
    }

    return {
      ok: response.ok,
      message: response.ok ? "Connected" : `HTTP ${response.status}`,
    };
  } catch (error) {
    return { ok: false, message: String(error) };
  }
}

export async function analyzeImage(
  image: string,
  config: ProviderConfig,
  prompt?: string
): Promise<SceneAnalysis> {
  const url = `${config.baseUrl}/chat/completions`;
  const model = config.visionModel || config.model;

  const systemPrompt =
    "Analyze wallpaper. Return strict JSON fields: " +
    "summary, subjects[], foreground[], background[], lighting, palette[], risks[].";

  const response = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${config.apiKey}`,
    },
    body: JSON.stringify({
      model,
      messages: [
        {
          role: "user",
          content: [
            { type: "text", text: prompt ? `${systemPrompt} Context: ${prompt}` : systemPrompt },
            { type: "image_url", image_url: { url: image } },
          ],
        },
      ],
    }),
  });

  if (!response.ok) {
    throw new Error(`Analysis failed: ${response.status}`);
  }

  const data = await response.json() as { choices?: Array<{ message?: { content?: string } }> };
  const content = data.choices?.[0]?.message?.content ?? "";

  // 解析 JSON
  const match = content.match(/\{[\s\S]*\}/);
  if (!match) throw new Error("No JSON found in response");

  return JSON.parse(match[0]) as SceneAnalysis;
}

export async function generateImage(
  image: string,
  prompt: string,
  width: number,
  height: number,
  config: ProviderConfig,
  negativePrompt?: string
): Promise<string> {
  // 使用自定义 generateUrl 或默认路径
  const url = config.generateUrl || `${config.baseUrl}/images/generations`;

  // OpenAI 兼容格式
  if (!config.generateUrl) {
    const response = await fetch(url, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${config.apiKey}`,
      },
      body: JSON.stringify({
        model: config.model,
        prompt,
        n: 1,
        size: `${width}x${height}`,
      }),
    });

    if (!response.ok) {
      throw new Error(`Generation failed: ${response.status}`);
    }

    const data = await response.json() as { data?: Array<{ url?: string }> };
    const imageUrl = data.data?.[0]?.url;
    if (!imageUrl) throw new Error("No image URL in response");
    return imageUrl;
  }

  // 阿里云 DashScope 格式
  const response = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${config.apiKey}`,
    },
    body: JSON.stringify({
      model: config.model,
      input: {
        messages: [
          {
            role: "user",
            content: [
              { image },
              { text: prompt },
            ],
          },
        ],
      },
      parameters: {
        negative_prompt: negativePrompt,
        size: `${width}*${height}`,
        watermark: false,
        n: 1,
      },
    }),
  });

  if (!response.ok) {
    throw new Error(`Generation failed: ${response.status}`);
  }

  const data = await response.json() as {
    output?: {
      choices?: Array<{
        message?: {
          content?: Array<{ image?: string }>;
        };
      }>;
    };
    code?: string;
    message?: string;
  };

  if (data.code) {
    throw new Error(`${data.code}: ${data.message}`);
  }

  const imageUrl = data.output?.choices?.[0]?.message?.content?.[0]?.image;
  if (!imageUrl) throw new Error("No image URL in response");
  return imageUrl;
}
```

- [ ] **Step 3: 提交**

```bash
git add worker/providers.ts worker/types.ts
git commit -m "feat: 实现 Provider 请求适配器

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 3.2: 完善 Worker API 路由

**Files:**
- Modify: `worker/index.ts`

- [ ] **Step 1: 更新 worker/index.ts**

```typescript
import { Hono } from "hono";
import { cors } from "hono/cors";
import { testConnection, analyzeImage, generateImage } from "./providers";
import type { AnalyzeRequest, GenerateRequest } from "./types";

const app = new Hono();

// CORS 中间件
app.use(
  "/api/*",
  cors({
    origin: "*",
    allowMethods: ["GET", "POST", "OPTIONS"],
    allowHeaders: ["Content-Type", "Authorization"],
  })
);

// 健康检查
app.get("/api/health", (c) => {
  return c.json({ status: "ok", timestamp: new Date().toISOString() });
});

// 测试连接
app.post("/api/test-connection", async (c) => {
  try {
    const body = await c.req.json<{ provider: { baseUrl: string; apiKey: string; model: string } }>();
    const result = await testConnection(body.provider);
    return c.json(result);
  } catch (error) {
    return c.json({ ok: false, message: String(error) }, 500);
  }
});

// 图像分析
app.post("/api/analyze", async (c) => {
  try {
    const body = await c.req.json<AnalyzeRequest>();
    const result = await analyzeImage(body.image, body.provider, body.prompt);
    return c.json(result);
  } catch (error) {
    return c.json({ error: String(error) }, 500);
  }
});

// 图像生成
app.post("/api/generate", async (c) => {
  try {
    const body = await c.req.json<GenerateRequest>();
    const imageUrl = await generateImage(
      body.image,
      body.prompt,
      body.width,
      body.height,
      body.provider,
      body.negativePrompt
    );
    return c.json({ imageUrl });
  } catch (error) {
    return c.json({ error: String(error) }, 500);
  }
});

export default app;
```

- [ ] **Step 2: 本地测试 Worker**

```bash
pnpm dev:worker
```

Expected: Worker 启动在 http://localhost:8787

- [ ] **Step 3: 测试 API**

```bash
curl http://localhost:8787/api/health
```

Expected: `{"status":"ok","timestamp":"..."}`

- [ ] **Step 4: 提交**

```bash
git add worker/index.ts
git commit -m "feat: 完善 Worker API 路由

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Chunk 4: 前端状态管理迁移

### Task 4.1: 迁移 useSettingsStore

**Files:**
- Modify: `src/store/useSettingsStore.ts`

- [ ] **Step 1: 重写 Provider 配置类型**

```typescript
// src/types/provider.ts
export interface ProviderTemplate {
  id: string;
  name: string;
  baseUrl: string;
  generateUrl?: string;
  defaultModel: string;
  defaultVisionModel?: string;
}

export interface ProviderConfig {
  templateId: string;
  baseUrl: string;
  apiKey: string;
  model: string;
  visionModel?: string;
  generateUrl?: string;
}

export const PROVIDER_TEMPLATES: ProviderTemplate[] = [
  {
    id: "ark",
    name: "火山引擎 Ark",
    baseUrl: "https://ark.cn-beijing.volces.com/api/v3",
    generateUrl: "https://ark.cn-beijing.volces.com/api/v3/images/generations",
    // 注意：实际使用需先部署模型，获得 ep- 开头的接入点ID
    defaultModel: "Doubao-Seedream-5.0-Lite",
    defaultVisionModel: "doubao-seed-2-0-lite",
  },
  {
    id: "aliyun",
    name: "阿里云百炼",
    baseUrl: "https://dashscope.aliyuncs.com/compatible-mode/v1",
    generateUrl: "https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation",
    defaultModel: "wanx2.1-t2i-turbo",
    defaultVisionModel: "qwen-vl-max",
  },
  {
    id: "openrouter",
    name: "OpenRouter",
    baseUrl: "https://openrouter.ai/api/v1",
    defaultModel: "google/gemini-2.0-flash-exp:free",
    defaultVisionModel: "google/gemini-2.0-flash-exp:free",
  },
  {
    id: "custom",
    name: "自定义",
    baseUrl: "",
    defaultModel: "",
  },
];
```

- [ ] **Step 2: 重写 useSettingsStore**

```typescript
// src/store/useSettingsStore.ts
import { create } from "zustand";
import { persist } from "zustand/middleware";
import type { ProviderConfig } from "@/types/provider";

interface SettingsState {
  provider: ProviderConfig;
  language: "zh" | "en";
  theme: "light" | "dark" | "system";
  setProvider: (provider: Partial<ProviderConfig>) => void;
  setLanguage: (language: "zh" | "en") => void;
  setTheme: (theme: "light" | "dark" | "system") => void;
}

export const useSettingsStore = create<SettingsState>()(
  persist(
    (set) => ({
      provider: {
        templateId: "openrouter",
        baseUrl: "https://openrouter.ai/api/v1",
        apiKey: "",
        model: "google/gemini-2.0-flash-exp:free",
        visionModel: "google/gemini-2.0-flash-exp:free",
      },
      language: "zh",
      theme: "system",
      setProvider: (provider) =>
        set((state) => ({ provider: { ...state.provider, ...provider } })),
      setLanguage: (language) => set({ language }),
      setTheme: (theme) => set({ theme }),
    }),
    {
      name: "wallpaperduo-settings",
    }
  )
);
```

- [ ] **Step 3: 提交**

```bash
git add src/store/useSettingsStore.ts src/types/provider.ts
git commit -m "refactor: 迁移设置存储，简化 Provider 配置

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 4.2: 迁移 useWorkflowStore

**Files:**
- Modify: `src/store/useWorkflowStore.ts`

- [ ] **Step 1: 审查现有 store**

阅读 `src/store/useWorkflowStore.ts`，理解当前状态结构。

- [ ] **Step 2: 迁移 store（保持核心逻辑）**

保留现有工作流状态，移除 MUI 相关导入，保持类型兼容。

- [ ] **Step 3: 提交**

```bash
git add src/store/useWorkflowStore.ts
git commit -m "refactor: 迁移工作流存储

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Chunk 5: 前端组件迁移

### Task 5.1: 创建 Provider 配置组件

**Files:**
- Create: `src/components/settings/ProviderConfig.tsx`

- [ ] **Step 1: 创建 ProviderConfig.tsx**

```tsx
import { useSettingsStore } from "@/store/useSettingsStore";
import { PROVIDER_TEMPLATES } from "@/types/provider";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
import { useState } from "react";

export function ProviderConfig() {
  const { provider, setProvider } = useSettingsStore();
  const [testing, setTesting] = useState(false);
  const [testResult, setTestResult] = useState<string | null>(null);

  const handleTemplateChange = (templateId: string) => {
    const template = PROVIDER_TEMPLATES.find((t) => t.id === templateId);
    if (template) {
      setProvider({
        templateId,
        baseUrl: template.baseUrl,
        model: template.defaultModel,
        visionModel: template.defaultVisionModel,
        generateUrl: template.generateUrl,
      });
    }
  };

  const handleTestConnection = async () => {
    setTesting(true);
    setTestResult(null);

    try {
      const response = await fetch("/api/test-connection", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ provider }),
      });
      const result = await response.json();
      setTestResult(result.ok ? "连接成功" : `失败: ${result.message}`);
    } catch (error) {
      setTestResult(`错误: ${error}`);
    } finally {
      setTesting(false);
    }
  };

  return (
    <div className="space-y-4">
      <div className="space-y-2">
        <Label>Provider 模板</Label>
        <Select value={provider.templateId} onValueChange={handleTemplateChange}>
          <SelectTrigger>
            <SelectValue />
          </SelectTrigger>
          <SelectContent>
            {PROVIDER_TEMPLATES.map((t) => (
              <SelectItem key={t.id} value={t.id}>
                {t.name}
              </SelectItem>
            ))}
          </SelectContent>
        </Select>
      </div>

      <div className="space-y-2">
        <Label>API Key</Label>
        <Input
          type="password"
          value={provider.apiKey}
          onChange={(e) => setProvider({ apiKey: e.target.value })}
          placeholder="sk-..."
        />
      </div>

      <div className="space-y-2">
        <Label>Base URL</Label>
        <Input
          value={provider.baseUrl}
          onChange={(e) => setProvider({ baseUrl: e.target.value })}
          placeholder="https://api.example.com/v1"
        />
      </div>

      <div className="space-y-2">
        <Label>图像生成模型</Label>
        <Input
          value={provider.model}
          onChange={(e) => setProvider({ model: e.target.value })}
          placeholder="model-name"
        />
      </div>

      <div className="space-y-2">
        <Label>视觉模型（可选）</Label>
        <Input
          value={provider.visionModel || ""}
          onChange={(e) => setProvider({ visionModel: e.target.value })}
          placeholder="vision-model-name"
        />
      </div>

      <Button onClick={handleTestConnection} disabled={testing || !provider.apiKey}>
        {testing ? "测试中..." : "测试连接"}
      </Button>

      {testResult && <p className="text-sm">{testResult}</p>}
    </div>
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add src/components/settings/ProviderConfig.tsx
git commit -m "feat: 创建 Provider 配置组件

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 5.2: 创建布局组件

**Files:**
- Modify: `src/components/layout/AppShell.tsx`
- Modify: `src/pages/MainPage.tsx`

- [ ] **Step 1: 使用 shadcn/ui 重写 AppShell**

迁移顶部导航栏，使用 shadcn/ui 的 Button、Switch 组件。

- [ ] **Step 2: 更新 MainPage 布局**

使用 Tailwind 的 flexbox/grid 布局替代 MUI Grid。

- [ ] **Step 3: 提交**

```bash
git add src/components/layout/AppShell.tsx src/pages/MainPage.tsx
git commit -m "refactor: 迁移布局组件到 shadcn/ui

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 5.3: 迁移核心业务组件

**Files:**
- Modify: `src/components/canvas/CanvasWorkspace.tsx`
- Modify: `src/components/control/ControlPanel.tsx`
- Modify: `src/components/results/ResultsRail.tsx`
- Modify: `src/components/results/ExportPanel.tsx`

- [ ] **Step 1: 迁移 CanvasWorkspace**

使用 Tailwind 样式，保留画布逻辑。

- [ ] **Step 2: 迁移 ControlPanel**

使用 shadcn/ui 组件重构控制面板。

- [ ] **Step 3: 迁移 ResultsRail**

使用 shadcn/ui Card 组件。

- [ ] **Step 4: 迁移 ExportPanel**

添加 ZIP 导出功能。

- [ ] **Step 5: 提交**

```bash
git add src/components/
git commit -m "refactor: 迁移核心业务组件

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Chunk 6: 删除废弃代码

### Task 6.1: 删除不需要的文件

**Files:**
- Delete: `src/services/generation/localFallback.ts`
- Delete: `src/services/export/heicExporter.ts`
- Delete: `src/services/providers/comfyuiProvider.ts`
- Delete: `src/theme/theme.ts`

- [ ] **Step 1: 删除本地回退生成**

```bash
rm src/services/generation/localFallback.ts
```

- [ ] **Step 2: 删除 HEIC 导出**

```bash
rm src/services/export/heicExporter.ts
```

- [ ] **Step 3: 删除 ComfyUI Provider**

```bash
rm src/services/providers/comfyuiProvider.ts
```

- [ ] **Step 4: 删除 MUI 主题**

```bash
rm src/theme/theme.ts
```

- [ ] **Step 5: 更新导入**

移除所有对删除文件的引用。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "refactor: 删除废弃代码（本地回退、HEIC、ComfyUI、MUI）

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

## Chunk 7: 集成测试与部署

### Task 7.1: 本地测试

- [ ] **Step 1: 安装依赖**

```bash
pnpm install
```

- [ ] **Step 2: 构建检查**

```bash
pnpm build
```

Expected: 构建成功，无错误

- [ ] **Step 3: 本地开发测试**

```bash
pnpm dev
pnpm dev:worker  # 新终端
```

Expected: 前端和 Worker 都启动成功

- [ ] **Step 4: 功能测试**

1. 打开 http://localhost:5173
2. 配置 Provider
3. 上传图片
4. 执行分析
5. 执行生成
6. 导出 ZIP/DDW

---

### Task 7.2: 部署

- [ ] **Step 1: 登录 Cloudflare**

```bash
pnpm wrangler login
```

- [ ] **Step 2: 部署**

```bash
pnpm deploy
```

Expected: 部署成功，显示 URL

- [ ] **Step 3: 验证部署**

访问部署的 URL，测试完整工作流。

---

## 验收清单

- [ ] Cloudflare Workers 部署成功
- [ ] 前端调用 `/api/*` 无 CORS 错误
- [ ] Provider 模板选择 → 预填 → 测试连接成功
- [ ] 完整工作流：上传 → 分析 → 生成 → 导出
- [ ] ZIP 导出正常
- [ ] DDW 导出正常
- [ ] 中英文切换正常
- [ ] 深浅主题切换正常
- [ ] `pnpm lint` 通过
- [ ] `pnpm build` 通过